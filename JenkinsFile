pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'node --version'
                sh 'npm --version'
            }
        }
        
        stage('Build') {
            agent  {
                docker {
                    image 'node:18-alpine'
                }
            }
            
            steps {
                cleanWs()
                script{
                    currentBuild.ChangeSets.Items.each {
                        echo it
                    }
                }
            
            }
        }
    }
}
